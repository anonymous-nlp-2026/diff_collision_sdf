# ColliForce-VLA Evaluation Plan

## Pre-flight Status (2026-05-20)

| Check | Result |
|-------|--------|
| eval_batch.sh --dry_run | PASS — 4 jobs resolve, A3 skipped (remote) |
| eval_safelibero.py import | PASS |
| norm_stats.json | PASS — embedded in each checkpoint's assets/ |

## Current Checkpoint Inventory

### <SERVER>
| Experiment | Steps Available | Latest |
|-----------|----------------|--------|
| baseline_spatial_bowl seed0 | 16000, 18000, best | 18000 |
| baseline_object_pudding seed0 | 18000, 20000, best | 20000 |
| a1_sdf_spatial_bowl seed0 | 14000, 16000, 18000, best | 18000 |
| a1_sdf_object_pudding seed0 | 2000, 4000, best | 4000 (training) |

### <SERVER>
| Experiment | Steps Available | Latest |
|-----------|----------------|--------|
| a3_grad_refine_spatial_bowl seed0 | 6000, 8000, 10000, best | 10000 |
| a3_grad_refine_object_pudding seed0 | 8000, 10000, 12000, best | 12000 |

## Eval Schedule

训练完成后立即在释放的 GPU 上启动 eval，不等全部完成。

### Phase 1: Baselines + A1 spatial (~May 21 06:00 UTC)
预计最先完成，训练已接近 30K。

- **baseline_spatial_bowl** → eval on cuda:0
  ```
  eval_safelibero.py --checkpoint .../baseline_spatial_bowl_seed0/<final_step>
    --config_name baseline_spatial_bowl --suite safelibero_spatial
    --safety_level II --n_episodes 50 --model_type baseline
  ```
- **baseline_object_pudding** → eval on cuda:2
  ```
  eval_safelibero.py --checkpoint .../baseline_object_pudding_seed0/<final_step>
    --config_name baseline_object_pudding --suite safelibero_object
    --safety_level I --n_episodes 50 --model_type baseline
  ```
- **a1_sdf_spatial_bowl** → eval on cuda:3
  ```
  eval_safelibero.py --checkpoint .../a1_sdf_spatial_bowl_seed0/<final_step>
    --config_name baseline_spatial_bowl --suite safelibero_spatial
    --safety_level II --n_episodes 50 --model_type a1
    --sdf_checkpoint .../a1_sdf_spatial_bowl_seed0/<final_step>/sdf_decoder.pt
  ```

### Phase 2: A1 object_pudding (~May 21 16:00 UTC)
当前仅 step 4000，距 30K 较远。

- **a1_sdf_object_pudding** → eval on cuda:1

### Phase 3: A3 (after scp from <SERVER>)
A3 训练在 <SERVER>，eval 环境在 <SERVER>。

- **a3_grad_refine_spatial_bowl** → scp final checkpoint to <SERVER>, eval
- **a3_grad_refine_object_pudding** → same

### Cross-Server Strategy
推荐方案 A: scp A3 checkpoint 到 <SERVER> eval（环境一致，SafeLIBERO 数据已在本地）。
```bash
# 从 <SERVER> 拷贝（示例）
scp -r root@<SERVER>:<DATA_ROOT>/checkpoints/a3_grad_refine_spatial_bowl/ \
    <DATA_ROOT>/checkpoints/a3_grad_refine_spatial_bowl/
```

## MVP Gate Check

| Comparison | Metric | Pass Criteria |
|-----------|--------|---------------|
| A3 vs Baseline | CAR | 下降 ≥15% |
| A3 vs Baseline | TSR | 损失 <10% |
| A1 vs Baseline | CAR | 改善（ablation evidence） |

## Eval Parameters
- n_episodes: 50 per scene
- max_steps: 400 (default)
- Scenes: safelibero_spatial (Level II), safelibero_object (Level I)
- Metrics: TSR, CAR, ETS, avg_smoothness
- Output: CSV per eval run (auto-generated by eval_batch.sh)
